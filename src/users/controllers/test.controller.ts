import { Controller } from '@nestjs/common';

@Controller()
export class TestController {

    /**
     * @example Example usage of leaveOnlyUnique method
     * // returns [1, 2, 5, 9]
     * this.leaveOnlyUnique([1, 5, 2, 5, 9, 1, 1, 2, 1, 9]);
     */

    leaveOnlyUnique(array) {

    }
}
